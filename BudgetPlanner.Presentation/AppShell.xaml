<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="BudgetPlanner.Presentation.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:BudgetPlanner.Presentation"
    Shell.FlyoutBehavior="Disabled"
    Title="BudgetPlanner.Presentation"
        xmlns:view = "clr-namespace:BudgetPlanner.Presentation.View"
    >


    <TabBar >
        <ShellContent Title="Einnahmen"
                     Icon="income.png" 
                     ContentTemplate="{DataTemplate view:IncomePage}" />
        <ShellContent Title="Ausgabe"
                     Icon="expenses.png" 
                     ContentTemplate="{DataTemplate view:ExpensesPage}" />
        <ShellContent Title="Übersicht"
                     Icon="budget.png"
                     ContentTemplate="{DataTemplate view:OverviewPage}" />

    </TabBar>


</Shell>
